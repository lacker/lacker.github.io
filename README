To get this set up on a new machine:

```
sudo apt-get install jekyll
gem install jekyll-paginate
```

To run the site locally:

`jekyll serve`

and go to `localhost:4000`

To deploy, just push to GitHub.

To create a new post, make a markdown file in `_posts`.
